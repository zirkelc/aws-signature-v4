{
  "extends": "@total-typescript/tsconfig/bundler/dom",
  "compilerOptions": {
    /* The library itself is environment agnostic, but the tests simulate a browser by
       stubbing Node globals, so the Node types have to be pulled in explicitly. */
    "types": ["node"]
  },
  "include": ["src/**/*.ts"]
}
